apiVersion: v1
kind: Namespace
metadata:
  name: sidewhale-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sidewhale-runtime
  namespace: sidewhale-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: sidewhale-runtime
  namespace: sidewhale-system
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "create", "delete", "patch"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["get", "create"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["networkpolicies"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: sidewhale-runtime
  namespace: sidewhale-system
subjects:
  - kind: ServiceAccount
    name: sidewhale-runtime
    namespace: sidewhale-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: sidewhale-runtime
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sidewhale
  namespace: sidewhale-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sidewhale
  template:
    metadata:
      labels:
        app: sidewhale
        sidewhale.io/component: control-plane
    spec:
      serviceAccountName: sidewhale-runtime
      containers:
        - name: sidewhale
          image: obegron/sidewhale:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          args:
            - --listen
            - :23750
            - --listen-unix
            - "-"
            - --state-dir
            - /var/lib/sidewhale
            - --runtime-backend
            - k8s
            - --enable-image-mutations=false
          ports:
            - containerPort: 23750
          startupProbe:
            tcpSocket:
              port: 23750
            periodSeconds: 5
            failureThreshold: 24
          readinessProbe:
            httpGet:
              path: /readyz
              port: 23750
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 23750
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: state
              mountPath: /var/lib/sidewhale
      volumes:
        - name: state
          persistentVolumeClaim:
            claimName: sidewhale-state
---
apiVersion: v1
kind: Service
metadata:
  name: sidewhale
  namespace: sidewhale-system
spec:
  selector:
    app: sidewhale
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - name: http
      port: 23750
      targetPort: 23750
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sidewhale-control-plane-ingress
  namespace: sidewhale-system
spec:
  podSelector:
    matchLabels:
      app: sidewhale
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              sidewhale.io/client: "true"
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sidewhale-workload-ingress
  namespace: sidewhale-system
spec:
  podSelector:
    matchLabels:
      sidewhale.managed: "true"
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: sidewhale
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sidewhale-state
  namespace: sidewhale-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
